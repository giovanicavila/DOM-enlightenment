<div id="spotify-body">
  <a href="#" class="sp-track" id="sp-link">
    <img src="https://i.scdn.co/image/ab67616d0000b2733cf83c82a8e976d7b51e2d00" alt="" class="sp-art" id="sp-art" />
    <div class="sp-body">
      <span class="sp-title" id="sp-title">It Ain't Like That</span>
      <span class="sp-album" id="sp-album">Facelift</span>
      <span class="sp-date" id="sp-date">Jun 17, 2026</span>
    </div>
  </a>
</div>

<style>
  .sp-track {
    display: flex;
    align-items: center;
    gap: 0.875rem;
    padding: 0.75rem 1rem;
    border-radius: 0.75rem;
    border: 1px solid var(--border-color);
    text-decoration: none;
    color: inherit;
    transition: background var(--transition-base);
  }

  .sp-track:hover {
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(255, 255, 255, 0.18);
  }

  .sp-art {
    width: 3rem;
    height: 3rem;
    border-radius: 0.375rem;
    object-fit: cover;
    flex-shrink: 0;
  }

  .sp-body {
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 0.125rem;
  }

  .sp-title {
    font-size: var(--text-sm);
    font-weight: 500;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .sp-album {
    font-size: var(--text-xs);
    opacity: 0.5;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .sp-date {
    font-size: 0.625rem;
    opacity: 0.35;
    letter-spacing: 0.01em;
  }
</style>
